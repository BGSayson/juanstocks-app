<div class="<%=admin_flex_col_container%> px-[1rem] mt-[1rem]">
  <h2 class="<%=admin_main_header%>">Forgot your password?</h2>
  <div class="<%=admin_divider%>"></div>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: {class:admin_flex_col_container, method: :post }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: admin_form_field%>
    </div>

    <div class="actions">
      <%= f.submit "Send me reset password instructions", class: welcome_primary_button %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>