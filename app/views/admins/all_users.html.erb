<%= render 'admins/admin_top_bar'%>
<div class="<%=admin_flex_col_container%> px-[1rem] mt-[4rem]">
  <div class="<%=admin_flex_row_container%> justify-between">
    <h1 class="<%=admin_main_header%>">All Juanstocks Users</h1>
    <%= button_to image_tag('icons/NewUser.png', height:21, width:21 ) + "New User", new_admin_user_path(current_user), method: :get, :class => admin_dashboard_button %>
  </div>
  <table class="<%=admin_table%>">
    <thead>
      <tr>
        <th>ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Role</th>
        <th>Status</th>
        <th>Confirmed Timestamp</th>
      </tr>
    </thead>
    <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %> </td>
        <td><%= user.first_name %> </td>
        <td><%= user.last_name %> </td>
        <td><%= user.email %> </td>
        <td><%= user.user_role %> </td>
        <td><%= user.user_status %> </td>
        <% if user.confirmed_at == nil   %>
        <td> Account not confirmed </td>
        <% else %>
        <td><%= user.confirmed_at %> </td>
        <% end %>
        <td><%= button_to image_tag('icons/Search.png', height:21, width:21 ), user_path(user.id), method: :get, :class=> admin_mini_button%></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>