Hello trader <%= current_user.first_name%>
<%= button_to "Sign out", destroy_user_session_path, method: :delete  %>

<%# TESTING WALLET PATHS %>
<% if(current_user.user_role == "admin" ) %>
  <%= button_to "All Transactions", wallet_transactions_path(current_user.wallet.id), method: :get  %>
<% else %>
  <%= button_to "Wallet", wallet_path(current_user.wallet.id), method: :get  %>
<% end %>